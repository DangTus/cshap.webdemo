@page
@model WebDemo.Pages.Students.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Student316[0].LastName316)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student316[0].FirstMidName316)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student316[0].EnrollmentDate316)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Student316) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LastName316)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstMidName316)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EnrollmentDate316)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
